web: uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000} --proxy-headers --forwarded-allow-ips "${FORWARDED_ALLOW_IPS:-*}" --ws-ping-interval ${WS_PING_INTERVAL:-20} --ws-ping-timeout ${WS_PING_TIMEOUT:-20} --timeout-keep-alive ${KEEP_ALIVE_TIMEOUT:-10}
